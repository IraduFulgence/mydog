export const FAVORITES_KEY = '@dog_favorites';
export const THEME_KEY = '@dog_theme';

export const API_CONSTANTS = {
  DEFAULT_LIMIT: 50,
  MAX_LIMIT: 100,
  IMAGE_QUALITY: 'med',
  ALLOWED_MIME_TYPES: 'jpg,png',
} as const;

export const ANIMATION_DURATION = {
  fast: 200,
  normal: 300,
  slow: 500,
} as const;